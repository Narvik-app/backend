{% extends 'email/layout.html.twig' %}

{% block css %}
    <style type="text/css" media="screen">
    .security-code {
        text-align: center;
        padding-top: 24px;
        padding-bottom: 24px;
    }

    .security-code > span {
        font-size: 20px;
        font-weight: bold;
        margin: 0 6px;
        padding: 10px;
        border: 1px solid #eaeaeb;
        border-radius: 24px;
    }
    </style
{% endblock %}

{% block content %}
    <p>Une action nécessitant un code de sécurité a été effectuée sur <a href="{{ frontend_url }}">Narvik</a></p>
    <br />

    <p>Votre code de sécurité est :</p>
    <div class="security-code">{% for code in security_code|split('') %}<span>{{ code }}</span>{% endfor %}</div>
    <p>Le code est valable 10 minutes.</p>

    {% if frontend_path is not empty %}
        <br />
        <p>
            Url de la demande: <a href="{{ frontend_url }}{{ frontend_path }}">{{ frontend_url }}{{ frontend_path }}</a>
        </p>
    {% endif %}
{% endblock %}
